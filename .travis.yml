sudo: false
language: python

install:
 - pip install -r requirements/common.txt
 - pip install -r requirements/development.txt

script:
 - nosetests --with-coverage --cover-html --cover-branches --cover-erase --cover-package=cartolafc_api

after_success:
  - coveralls
